/**
 * Must match `tabBarStyle.height` in `app/(tabs)/_layout.tsx`.
 * Tab bar safe-area padding is handled by React Navigation; this is the styled bar height only.
 */
export const MAIN_TAB_BAR_HEIGHT = 72;
